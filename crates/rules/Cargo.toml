[package]
name = "aegislens-rules"
version = "0.1.0"
edition = "2021"

[lib]
name = "aegislens_rules"
path = "src/lib.rs"

[dependencies]
aegislens-core = { path = "../core" }
aegislens-packet = { path = "../packet" }
